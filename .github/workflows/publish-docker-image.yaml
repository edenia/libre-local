name: Publish Docker image

on:
  push:
    branches: master

jobs:
  push_to_registries:
    name: Push Docker image to multiple registries
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Log in to the Container registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v3
        with:
          images: |
            eoscostarica506/phoenix-local
            ghcr.io/${{ github.repository }}

      - name: Build new images and push
        run: |
          make \
            build-docker \
            push-image
        env:
          TESTNET_EOSIO_PRIVATE_KEY: 5JXUDgNULcdt3VJiwz9RhrQKpEqmvXEduYB9iLYmPeJyFaVFf97
          TESTNET_EOSIO_PUBLIC_KEY: EOS54XsQUccmfiaLFybXogaqGSApbBNWFkKqUa7595KC91PyNG7xu
          DOCKER_REGISTRY: eoscostarica506
          IMAGE_NAME: phoenix-local
